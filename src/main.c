#include <stdio.h>
#include <stdlib.h>
#include "lab.h"


int main(int argc, char **argv)
{

  if (argc < 3)
    {
      printf("usage: %s <array_size> <num_threads>", argv[0]);
      return 1;
    }
  //TODO: UPDATEME
  
  //Format for script
 printf("%d %f\n", t, end - start);

  return 0;
}